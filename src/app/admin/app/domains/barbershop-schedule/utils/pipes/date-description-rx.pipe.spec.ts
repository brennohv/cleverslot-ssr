import { DateDescriptionRxPipe } from './date-description-rx.pipe';

describe('DateDescriptionRxPipe', () => {
  it('create an instance', () => {
    const pipe = new DateDescriptionRxPipe();
    expect(pipe).toBeTruthy();
  });
});
