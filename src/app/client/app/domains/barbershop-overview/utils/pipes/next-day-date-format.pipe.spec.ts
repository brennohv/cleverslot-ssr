import { NextDayDateFormatPipe } from './next-day-date-format.pipe';

describe('NextDayDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new NextDayDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
